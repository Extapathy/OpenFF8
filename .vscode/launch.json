{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug OpenFF8",
            "type": "cppvsdbg",
            "request": "attach",
            "windows": {},
            "preLaunchTask": "inject dll",
            "processId": "${command:pickProcess}"
        }
    ]
}